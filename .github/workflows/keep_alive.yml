name: Keep Render Awake

on:
  schedule:
    # 5분마다
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -L https://joblens-xwz9.onrender.com/
